
spring:

  datasource:
    url: jdbc:h2:file:~/db/swegger
    username: sa
    password:


  jpa:
    show-sql: false
    open-in-view: true
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop